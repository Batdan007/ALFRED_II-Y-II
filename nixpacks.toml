# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-railway.txt"]

[start]
cmd = "uvicorn maiai_platform.server:app --host 0.0.0.0 --port $PORT"
